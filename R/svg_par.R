

svg_par_instance <- list(
  units = 'px',
  width = 400,
  height = 300,
  font_size = 30,
  margin = c(5.1,4.1, 4.1, 2.1)
)


#stroke_width values meaning depends on stroke_width_type
# relative is a percentage of the min(width, height)
# absolute means that stroke-width should be a character with a number + units
svg_par_points <- list(
  fill = 'white',
  stroke = 'black',
  stroke_width_type = 'relative',
  stroke_width = 0.01
)


